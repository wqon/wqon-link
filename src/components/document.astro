<a class="home" href="/" data-cursor="pointer">back to links</a>
<slot />
<a class="wn" href="/" data-cursor="pointer"></a>

<style is:global>
    body {
        background: var(--black);
        color: var(--orange);
    }

    .home {
        text-align: center;
        margin-bottom: var(--32);
        z-index: 9;
    }
    .home:hover {
        color: var(--white);
    }

    section {
        display: grid;
        align-items: center;
        justify-content: center;
        margin: var(--64) 0;
    }
    pre {
        width: 768px;
        background: var(--orange);
        color: var(--darkblue);
        font-family: "Petrona", monospace;
        font-weight: 300;
        font-size: 1.125rem;
        white-space: pre-line;
        border-radius: calc(1rem / 4);
        box-shadow: 0 0 96px var(--blue);
        /* animation: Pulse 1s alternate infinite ease-in-out; */
        padding: var(--48);
        user-select: text;
        z-index: 8;
    }
    /* @keyframes Pulse {
        from {
            box-shadow: 0 0 48px var(--blue);
        }
        to {
            box-shadow: 0 0 96px var(--blue);
        }
    } */

    .wn {
        --wn: calc(7px * 4);
        display: block;
        mask: url("/assets/wnDarkOC.png") center / cover;
        width: var(--wn);
        height: var(--wn);
        image-rendering: pixelated;
        margin: var(--48) auto 0 auto;
        animation: Rainbow 30s linear infinite;
        transition: transform 0.4s ease-in-out;
    }
    .wn:hover {
        transform: scale(1.125);
    }
    @keyframes Rainbow {
        100% {
            background: #e896f8;
        }
        85% {
            background: #f45252;
        }
        68% {
            background: #f29a36;
        }
        51% {
            background: #f2d636;
        }
        34% {
            background: #0edd49;
        }
        17% {
            background: #4e85f4;
        }
        0% {
            background: #e896f8;
        }
    }

    @media /* mobile */ (min-width: 1px) and (max-width: 768px) {
        pre {
            border-radius: 0;
            padding: var(--32);
        }
    }
</style>
